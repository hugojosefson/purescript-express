{
  "name": "purescript-express-example",
  "dependencies": {
    "body-parser": "~1.13.2",
    "express": "~4.13.0",
    "pulp": "^9.0.1",
    "purescript": "^0.9.1"
  },
  "scripts": {
    "start": "npm run install-deps && pulp run -m ToDoServer",
    "install-deps": "npm install && bower install"
  },
  "devDependencies": {
    "bower": "^1.7.9"
  }
}
